import { StyleSheet } from 'react-native'; 

export default styles = StyleSheet.create({
    inputs: {
        borderWidth: 2,
        borderColor: 'black',
        borderRadius: 10,
        padding: 5,
    }
})